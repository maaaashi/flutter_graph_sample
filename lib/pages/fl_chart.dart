import 'package:flutter/material.dart';

class FlChartPage extends StatelessWidget {
  const FlChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('FL Chart Page')));
  }
}
